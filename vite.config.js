import { defineConfig } from 'vite';

// Vite is used only for the dev server (npm run dev).
// The production build is handled by build.js (npm run build).
export default defineConfig({
  base: '/'
});
